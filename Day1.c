#include <stdio.h>

int main(void) {
    int a,b,sum;//variable declaration
    printf("Enter two numbers to add:\n");
    scanf("%d %d",&a,&b);
    sum=a+b;// addition
    printf("\nTotal is %d",sum);
  
  return 0;
}
